# CPU 后端示例项目配置
[package]
name = "cpu_demo"
version = "0.1.0"
edition = "2026"

[target]
device = "cpu"
arch = "x86_64"

[backend]
kind = "cpu"
optimization_level = 2
debug = false

[backend.cpu]
openmp = true
simd = true
num_threads = 0  # 0 表示自动检测
